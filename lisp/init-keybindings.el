;;; init-keybindings.el --- Custom key bindings

;;; Commentary:
;; Keep all key bindings in a single file
;; NOTE: Consider changing this to some sort of org-mode list that
;;       can be processed into a pdf cheatsheet

;;; License:

;;; Code:

(global-set-key (kbd "C-x C-r") 'recentf-open-files)


(provide 'init-keybindings)
;;; init-keybindings.el ends here
